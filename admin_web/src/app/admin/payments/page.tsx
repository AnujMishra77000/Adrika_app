export default function AdminPaymentsPage() {
  return (
    <section>
      <h1 style={{ marginTop: 0 }}>Payments</h1>
      <p className="muted">
        Payments module has been reset and removed from the current build. We will rebuild this flow from scratch in the
        new fee architecture.
      </p>
    </section>
  );
}
